[project]
name = "autonomous-writer"
version = "0.1.0"
description = "Autonomous AI blogging agent"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "openai>=1.0",
    "tavily-python>=0.5",
    "atproto==0.0.65",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
